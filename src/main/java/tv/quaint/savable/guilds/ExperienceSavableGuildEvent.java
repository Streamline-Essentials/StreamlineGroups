package tv.quaint.savable.guilds;

import tv.quaint.savable.GroupEvent;
import tv.quaint.savable.SavableGroup;

public class ExperienceSavableGuildEvent extends GuildGroupEvent {
    public ExperienceSavableGuildEvent(SavableGuild group) {
        super(group);
    }
}
